import ComputerPlayer from "./computer_player";

export default class DFSPlayer extends ComputerPlayer {
  // constructor(x, y) {
  //   super(x, y);
  // };

  // map = []
  
  algorithm(x, y, grid) {

    return [newX, newY]
  };
};